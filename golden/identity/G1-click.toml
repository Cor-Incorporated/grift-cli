# Provenance: this domain-separated digest was derived from public commit
# metadata in the click repository solely for tenant-scope golden replay.
# The plaintext address is intentionally not distributed.
# 削除依頼先 / removal requests: open an issue on the public repository
schema_version = "identity-v2"

# Pseudonymous canonical_id. Digest contract:
# sha256("tep-email-v1\0" + email.strip().lower()).
[tenant]
email_patterns = []

[[actors]]
canonical_id = "maintainer-1"
email_sha256 = ["a728a6577ae42a0e375ab4481c6f87c1952f324587a252ddc022547c20c06a2f"]
attribution_state = "verified"
