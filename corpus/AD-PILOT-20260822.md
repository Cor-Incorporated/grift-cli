# A/D 判別力パイロット（2026-08-22）

基準は実行前に `docs/corpus-protocol.md` へ登録済み（`5f2665e`）。
本ファイルは結果のみ。基準は動かしていない。

実行: `python3 scripts/corpus_ad_pilot.py`（golden cache、生存スキャンなし）

## 判定（事前基準の適用）

| 層 | 事前基準 | 結果 |
|---|---|---|
| Tier 1 観測状態 | D は co-change が not_observed。identity 付き A は narrate_rate | **pass** |
| Tier 2 率・効果量 | 両群 n≥2 のとき中央値差≥0.10・区間包含なし・Cliff δ≥0.33 | **inconclusive_small_n**（A narratable=1, D=0） |

率の判別力は、このピン集合ではまだ宣言しない。D は低率ではなく「未観測」で分かれており、それは Tier 1 の事前方向どおり。click の 0.2664 を D の欠測と引き算して効果量にしたわけではない。

## 行（リポ名のみ。個人名なし）

| 群 | リポ | test_frameworks | test_cochange | corrective_rework |
|---|---|---|---|---|
| A | click | pytest | 0.2664 (n=274, narrate) | 0.0109（fresh-work stability。バグ件数ではない） |
| A | express | mocha, nyc | not_observed (pending_attribution) | not_observed (pending_attribution) |
| A | typer | pytest | not_observed (pending_attribution) | not_observed (pending_attribution) |
| D | gitignore | not_observed | not_observed (no_test_framework_or_directory) | not_observed (pending_attribution) |
| D | Spoon-Knife | not_observed | not_observed (no_test_framework_or_directory) | not_observed (pending_attribution) |

## C pin（選定のみ。本パイロットでは未計測）

- C1-vibe: `https://github.com/mabene/vibe.git` @ `c53ab3cb3461ef43bb1b6336da4e9cdd477c63ac`
- 理由: README が「ソースは AI 対話で生成、人間が直接書いていない」と自己記述している。個人スコアカードには使わない。

## 次

Tier 2 を閉じるには、identity 付き A をあと1件以上、および（必要なら）テスト基盤があり tenant が測れる D ではない別ピンか、C との corrective 比較。基準の数値は変えない。
